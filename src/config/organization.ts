/**
 * Canonical Shepherd Neame Organization strap (ORG_DESCRIPTION)
 * 
 * This is the single source of truth for the Shepherd Neame organization description.
 * It is used in:
 * - Schema generation (Organization node description property)
 * - Schema Quality Charter document
 * - Rules UI (read-only display)
 * 
 * Only update this in one place - changes here will propagate to all uses.
 */
export const ORG_DESCRIPTION =
  "Shepherd Neame Limited is listed on the Aquis Stock Exchange and is Britain's oldest brewer, based in Faversham, Kent. It owns and operates a large estate of pubs and hotels across Kent, London and the South East.";
